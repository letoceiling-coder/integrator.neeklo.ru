# Regional Center

Interactive regional intelligence — wraps Stage 3 `RegionalIntelligenceEngine`.

## API

`GET /api/commerce/regions` (auto-refresh rankings)

## Per region

CTR, ROI, demand, supply, competition, opportunityIndex, marketHealth, rank

UI: `/analytics/regional` card grid with health badges.
