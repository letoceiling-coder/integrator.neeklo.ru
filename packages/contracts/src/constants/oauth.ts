/** Canonical OAuth callback path registered in Avito Developer Portal. */
export const OAUTH_CALLBACK_PATH = '/api/auth/os/callback';

/** Production redirect URI (API host + callback path). */
export const OAUTH_CALLBACK_URI_PRODUCTION = 'https://integrator.neeklo.ru/api/auth/os/callback';
