import { Injectable } from '@nestjs/common';

/**
 * Workflow AI helpers — AI nodes in Workflow Engine (action, decision, planner).
 * Event workflows that need commerce side-effects are registered in CommerceBootstrapService.
 */
@Injectable()
export class WorkflowAiService {}
